{
  "name": "json-server",
  "version": "1.0.0",
  "description": "json bda group 4",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lamsyah",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
